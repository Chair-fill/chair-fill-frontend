# ─────────────────────────────────────────────────────────────
# Environment Configuration
# ─────────────────────────────────────────────────────────────
# Copy this file to .env.local for local development.
# Do NOT commit .env.local to version control.

# Backend API base URL (must include NestJS global prefix, e.g. /api/v1)
# Example: http://localhost:3001/api/v1
NEXT_PUBLIC_API_URL=http://localhost:3001/api/v1

# ─────────────────────────────────────────────────────────────
# Feature Flags (NEXT_PUBLIC_FF_<FLAG_NAME>)
# ─────────────────────────────────────────────────────────────
# Set to "true" to enable, "false" to disable, or omit to use default.
# See lib/config/feature-flags.ts for all available flags and defaults.

# Demo Mode: Allow app usage without a real backend (default: false)
# Enable this for demos, testing, or development without backend.
NEXT_PUBLIC_FF_DEMO_MODE=false
